# CI-only requirements (light dependencies that install reliably on GitHub Actions)
# Heavy frameworks (tensorflow, torch, torch-geometric) are installed separately
# with continue-on-error since they may have platform/version issues.

# --- Core Scientific ---
numpy>=1.24
scipy>=1.10
pandas>=1.5

# --- Visualization ---
matplotlib>=3.7

# --- ML (lightweight) ---
scikit-learn>=1.2

# --- Sensitivity Analysis & UQ ---
SALib>=1.4
statsmodels>=0.14

# --- Testing ---
pytest>=7.3
pytest-cov>=4.0

# --- Utilities ---
tqdm>=4.65
PyYAML>=6.0
tabulate>=0.9.0
meshio>=5.3
